{
  "name": "grok-starter",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "pages:dev": "wrangler pages dev --compatibility-date=2024-11-04 --proxy 3000 -- astro dev",
    "pages:deploy": "astro build && wrangler pages deploy ./dist"
  },
  "dependencies": {
    "@ai-sdk/openai": "^0.0.71",
    "@astrojs/cloudflare": "^11.2.0",
    "ai": "^3.4.33",
    "astro": "^4.16.9"
  }
}
